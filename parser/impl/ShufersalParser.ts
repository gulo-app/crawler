import { Parser } from "../Parser";

class ShufersalParser extends Parser {

    constructor(name: string) {
        super(name);
    }

    parse(): void {
        //do parse
    }

    extractUrls(): Array<string> {
        return undefined;
    }
}