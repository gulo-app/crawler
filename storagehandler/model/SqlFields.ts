export enum SqlFields {
    BRAND_NAME = "brand_name",
    CATEGORY = "category_id",
    BARCODE = "barcode",
    PRODUCT_NAME = "product_name",
    BRAND_ID = "brand_id",
    CAPACITY = "capacity",
    CAPACITY_UNIT = "capacity_unit_id"
}

export enum ShoppingCartField {
    FIRM_ID = "shopping_cart_firm_id",
    BARCODE = "barcode",
    PRICE = "price",
    LINK = "link"
}

export enum ProductCategoryField {
    CATEGORY_ID = "category_id",
    BARCODE = "barcode"
}

export enum BrandsField {
    BRAND_ID = "brand_id",
    BRANDE_NAME = "brande_name"
}

